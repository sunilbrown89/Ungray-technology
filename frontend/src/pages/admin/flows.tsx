import { AdminLayout } from "@/layouts";

const flows = () => {
  return (
    <AdminLayout title="Flows">
      <section className="main-container">
        <p>Flows Page Under Progress</p>
      </section>
    </AdminLayout>
  );
};

export default flows;
