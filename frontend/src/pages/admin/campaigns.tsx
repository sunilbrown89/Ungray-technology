import { AdminLayout } from "@/layouts";

const campaigns = () => {
  return (
    <AdminLayout title="Campaigns">
      <section className="main-container">
        <p>Campaigns Page under progress</p>
      </section>
    </AdminLayout>
  );
};

export default campaigns;
