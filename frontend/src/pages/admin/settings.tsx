import { AdminLayout } from "@/layouts";

const settings = () => {
  return (
    <AdminLayout>
      <section className="main-container">
        <p>Settings Page Under Progress</p>
      </section>
    </AdminLayout>
  );
};

export default settings;
